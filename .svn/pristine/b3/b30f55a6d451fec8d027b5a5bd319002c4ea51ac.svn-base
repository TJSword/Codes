<view class="demand_wrap">
  <view class="left_title" style="margin-top:{{statusBarHeight}}px" bindtap = "goBack">
    <view class="iconfont icon-fanhui"></view>
    <view class="title">合同详情</view>
  </view>
  <view class="detailBox">
    <view class="title">{{contractInfo.contractName}}</view>
    <view class="pro_title_box">
      <view class="iconfont icon-xiangmu"></view>
      <view class="pro_title">{{contractInfo.projectName}}</view>
    </view>
    <view class="line"></view>
    <view class="pro_title_box">
      <view class="iconfont icon-gongsi"></view>
      <view class="pro_title">{{contractInfo.contactCorp}}</view>
    </view>
    <view class="line"></view>
    <view class="label_rp_box">
        <view class="label_box">
          <view class="iconfont icon-fl-renyuan"></view>
          <view class="label">{{contractInfo.customName}}</view>
        </view>
        <view class="commission_box">
          <view class="commission_icon iconfont icon-meiyuanbizhuanyongzhanghu"></view>
          <view class="commission_num">{{contractInfo.amount}}</view>
        </view>
    </view>
    <view class="line"></view>
    <view class="label_rp_box">
        <view class="label_box">
          <view class="iconfont icon-nanyichengdu"></view>
          <view class="label">华润</view>
        </view>
        <view class="commission_box">
          <view class="commission_icon iconfont icon-bianhao"></view>
          <view class="commission_num">{{contractInfo.contractNum}}</view>
        </view>
    </view>
    <view class="line"></view>
    <view class="time_box">
      <view class="iconfont icon-lipeishijian"></view>
      <view class="time_state">
        <view class="time">{{contractInfo.startDate}}  <text style="margin:0 10rpx">——</text>   {{contractInfo.endDate}}</view>
      </view>
    </view>
    <view class="line"></view>
    <view class="payment_box">
        <view class="payment" wx:for="{{payList}}">
          <view class="label">第{{index*1+1}}次付款：{{item.payDate}}</view>
          <view class="commission_num" style="margin-left:30rpx">金额：{{item.payAmount}}</view>
        </view>
    </view>
    <view class="line"></view>
    <view class="image_box">
      <image class="image" wx:for="{{fileArr}}"  mode="aspectFill" src="{{item}}" bindtap="preview"></image>
    </view>
  </view>
  <view class="loading" wx:if="{{loading}}">
    <view class="loader_box">
       <text class="loader"><text class="loader-inner"></text></text>
    </view>
  </view>
  <view class="model" wx:if="{{showBox}}">
    <view class="guanbi" bindtap="hideBox">×</view>
    <textarea name="" id="" bindinput="demand_evaluate" placeholder="请填写超期原因（必填）" cols="30" rows="10" class="textarea"></textarea>
    <view class="publish_btn" bindtap="submit">确认</view>
  </view>
  <view class="model" wx:if="{{showBox_daoshi}}">
    <view class="guanbi" bindtap="hideBox_daoshi">×</view>
    <textarea name="" id="" bindinput="demand_evaluate_daoshi" placeholder="导师评价（必填）" cols="30" rows="10" class="textarea"></textarea>
    <view class="publish_btn" bindtap="submit_daoshi">确认</view>
  </view>
</view>